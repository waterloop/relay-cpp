#include <config.h>

int main(int argc, char *argv[]) { 
    struct config configuration;
    if (argc > 1) { 
        // construct configuration from cmd line arguments
    } else {
        // construct configuration from default fn
    }
    // ...
}